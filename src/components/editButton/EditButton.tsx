"use client";
import { FC, useState } from "react";
import styles from "./editButton.module.scss";

export const EditButton: FC = ({}) => {
  return <button className={styles.ButtonWrapper}>РЕДАГУВАТИ</button>;
};
